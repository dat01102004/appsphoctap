name: appsshoptap

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  flutter_secure_storage: ^9.0.0
  image_picker: ^1.0.7
  flutter_tts: ^3.8.5
  speech_to_text: 7.0.0
  permission_handler: ^11.3.1
  provider: ^6.1.2

flutter:
  uses-material-design: true
  assets:
    - assets/icons/